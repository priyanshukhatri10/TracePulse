# TracePulse
TracePulse is a high-performance network intelligence engine that analyzes PCAP traffic, identifies applications and domains through deep packet inspection (including TLS SNI extraction), and applies policy-based blocking by IP, app, or domain through a scalable multithreaded pipeline with actionable traffic insights and reporting.
